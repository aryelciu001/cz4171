events {} 

http {
  server {
    listen 80;
    server_name _;
    location / {
      root /var/www/build;
    }
    location  /api {
      rewrite ^/api/?(.*)$ /$1 break;
      proxy_pass http://localhost:3001/;
    }
  }
}